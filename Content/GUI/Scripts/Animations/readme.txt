Animation scripting in lua:
The "OnLoad" function allows for loading of resources when the script is also first loaded.
The "Start" function runs all code within when the animation is called to be run once.